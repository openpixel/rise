{
  "i": ${i},
  "j": "${join(", ", h)}",
  "other": "${upper("foobar")}",
  "foo": "${join(" | ", list("foo", "bar"))}",
  "len": ${length(h)},
  "list": ${jsonencode(foo)},
  "map": ${jsonencode(bar)},
  "string": ${jsonencode(bar["this"])}
}
